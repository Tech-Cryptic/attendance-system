services:
  - type: web
    name: faceattend
    runtime: python
    rootDir: backend
    # 1. Build the React frontend, 2. Install Python deps
    buildCommand: >
      cd ../frontend &&
      npm ci &&
      npm run build &&
      cd ../backend &&
      pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    plan: free
    region: oregon
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: faceattend-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: HMAC_KEY
        generateValue: true
      - key: JWT_ALGORITHM
        value: HS256
      - key: JWT_EXPIRY_MINUTES
        value: "60"
      # Same origin — no CORS needed in production, but kept for safety
      - key: CORS_ORIGINS
        value: "*"
      - key: PYTHON_VERSION
        value: "3.11.0"
      # Tells seed.py the correct URL for enrollment links
      - key: FRONTEND_URL
        sync: false   # filled in after first deploy (use the Render service URL)

databases:
  - name: faceattend-db
    plan: free
    region: oregon
    databaseName: attendance_db
    user: postgres
